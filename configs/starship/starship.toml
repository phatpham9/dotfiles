[custom.git_user]
command = """
email=$(git config user.email | grep -v 'phat@onroads.xyz')
if [[ -n "$email" ]]; then
    echo "🏢 $email"
else
    exit 1
fi
"""
when = "git rev-parse --is-inside-work-tree 2> /dev/null"
style = "bold yellow"
format = "[$output]($style) "
